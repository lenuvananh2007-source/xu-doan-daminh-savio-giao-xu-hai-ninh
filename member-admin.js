(()=>{
// Production sync: 2026-08-26 — web cũ đã kết nối GitHub.
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const isAdmin=()=>!!(window.TNTT?.roles||[]).includes('admin');
window.openMemberProfile=async id=>{if(!window.sb)return;const {data:m,error}=await sb.from('members').select('*').eq('id',id).single();if(error)return alert(error.message);let modal=document.getElementById('memberAdminModal');if(!modal){modal=document.createElement('div');modal.id='memberAdminModal';modal.className='auth-overlay';document.body.appendChild(modal)}const field=(label,key,type='text')=>`<label>${label}<input class="input" data-member-field="${key}" type="${type}" value="${esc(m[key]||'')}" ${isAdmin()?'':'disabled'}></label>`;modal.innerHTML=`<div class="auth-card" style="max-width:800px"><button class="auth-close" onclick="memberAdminModal.remove()">×</button><h2>Hồ sơ thành viên</h2><div class="admin-form-grid">${field('Tên thánh','saint_name')}${field('Họ và tên','full_name')}${field('Ngày sinh','dob','date')}${field('Giới tính','gender')}${field('SĐT','phone')}${field('Tên phụ huynh/người giám hộ','parent_name')}${field('SĐT phụ huynh','parent_phone')}${field('Ngành','branch')}${field('Đội','team')}${field('Vai trò','team_role')}${field('Lớp giáo lý','catechism_class')}${field('Trạng thái','status')}</div><label>Ghi chú<textarea class="input" data-member-field="note" ${isAdmin()?'':'disabled'}>${esc(m.note||'')}</textarea></label>${isAdmin()?`<div class="auth-actions"><button class="primary" onclick="saveMemberProfile('${id}')">💾 Lưu cập nhật</button><button class="secondary" onclick="memberAdminModal.remove()">Đóng</button></div>`:'<p class="muted">Bạn chỉ có quyền xem hồ sơ này.</p>'}</div>`};
window.saveMemberProfile=async id=>{if(!isAdmin())return alert('Chỉ Admin được sửa hồ sơ.');const patch={};document.querySelectorAll('#memberAdminModal [data-member-field]').forEach(x=>patch[x.dataset.memberField]=x.value.trim()||null);const {error}=await sb.from('members').update(patch).eq('id',id);if(error)return alert(error.message);document.getElementById('memberAdminModal')?.remove();alert('Đã lưu cập nhật.');const active=document.querySelector('[data-route="members"].active');if(active)active.click();else if(window.members)members('Tất cả thành viên')};
async function enhance(){const page=document.getElementById('page');if(!page||document.getElementById('pageTitle')?.textContent!=='Thành viên')return;const table=page.querySelector('.table');if(!table||table.dataset.memberEnhanced)return;table.dataset.memberEnhanced='1';const head=table.querySelector('thead tr');if(!head)return;['Tên phụ huynh','SĐT phụ huynh','Thao tác'].forEach(t=>{const th=document.createElement('th');th.textContent=t;head.appendChild(th)});const names=[...table.querySelectorAll('tbody tr td:first-child b')].map(x=>x.textContent.trim()).filter(Boolean);if(!names.length)return;const {data,error}=await sb.from('members').select('id,full_name,parent_name,parent_phone').in('full_name',names);if(error)return;const buckets={};(data||[]).forEach(x=>(buckets[x.full_name]??=[]).push(x));[...table.querySelectorAll('tbody tr')].forEach(r=>{const name=r.querySelector('td:first-child b')?.textContent.trim();const m=buckets[name]?.shift();if(!m)return;const p=document.createElement('td');p.textContent=m.parent_name||'Chưa cập nhật';const ph=document.createElement('td');ph.textContent=m.parent_phone||'Chưa cập nhật';const a=document.createElement('td');a.innerHTML=`<button class="secondary" onclick="openMemberProfile('${m.id}')">${isAdmin()?'✏️ Sửa':'👁 Xem'}</button>`;r.append(p,ph,a)})}
let timer;const schedule=()=>{clearTimeout(timer);timer=setTimeout(enhance,120)};document.addEventListener('DOMContentLoaded',()=>{const p=document.getElementById('page');if(p)new MutationObserver(schedule).observe(p,{childList:true,subtree:true});schedule()});window.addEventListener('tntt-auth-changed',schedule);
})();